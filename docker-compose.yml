version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventory-app
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - NEXTAUTH_URL=https://inventory.insert.team
      - NEXTAUTH_SECRET=EtiO5rLn07EmxH9DDApC4eBBz0mr/6ArHEaF4n2MguI=
      - AUTH_TRUST_HOST=true
    volumes:
      # Persist SQLite DB only (not the entire prisma directory)
      - ./prisma/dev.db:/app/prisma/dev.db
    restart: always
